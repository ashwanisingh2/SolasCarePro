const { app, BrowserWindow } = require('electron'); app.whenReady().then(() => { const win = new BrowserWindow({ show: false, webPreferences: { nodeIntegration: true, contextIsolation: false } }); win.webContents.on('console-message', (event, level, message) => { console.log('RENDERER:', message); }); win.loadURL('http://localhost:5173'); setTimeout(() => { app.quit(); }, 15000); });
