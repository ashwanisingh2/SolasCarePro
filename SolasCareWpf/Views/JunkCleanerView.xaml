<UserControl x:Class="SolasCareWpf.Views.JunkCleanerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:SolasCareWpf.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="Transparent" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Junk Files Cleaner" Style="{StaticResource MaterialDesignHeadline4TextBlock}" Foreground="White" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Main Body -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Storage details list -->
            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                <!-- Temp files block -->
                <Border Style="{StaticResource GlassPanelStyle}" Background="#1E293B" Padding="16,14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Grid.Column="0" Kind="FolderOpen" Foreground="#F87171" Width="24" Height="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Temp Files" Foreground="White" FontWeight="Bold" FontSize="14"/>
                            <TextBlock Text="System and user temp cache" Foreground="#94A3B8" FontSize="11"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="{Binding TempFilesSize}" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Recycle Bin block -->
                <Border Style="{StaticResource GlassPanelStyle}" Background="#1E293B" Padding="16,14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Grid.Column="0" Kind="TrashCan" Foreground="#F87171" Width="24" Height="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Recycle Bin" Foreground="White" FontWeight="Bold" FontSize="14"/>
                            <TextBlock Text="Items in system recycle bins" Foreground="#94A3B8" FontSize="11"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="{Binding RecycleBinSize}" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Logs Cache block -->
                <Border Style="{StaticResource GlassPanelStyle}" Background="#1E293B" Padding="16,14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Grid.Column="0" Kind="FileDocument" Foreground="#F87171" Width="24" Height="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Log Cache Files" Foreground="White" FontWeight="Bold" FontSize="14"/>
                            <TextBlock Text="Windows updates and system logs" Foreground="#94A3B8" FontSize="11"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="{Binding LogCacheSize}" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Refresh and Clean Buttons -->
                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Style="{StaticResource MaterialDesignOutlinedButton}"
                            Command="{Binding RefreshSizesCommand}"
                            IsEnabled="{Binding IsCleaning, Converter={StaticResource InverseBoolConverter}}"
                            BorderBrush="#94A3B8" Foreground="White" Margin="0,0,6,0" Height="40">
                        <TextBlock Text="Scan Sizes" FontSize="12" FontWeight="Bold"/>
                    </Button>
                    <Button Grid.Column="1" Style="{StaticResource MaterialDesignFlatMidPrimaryButton}"
                            Command="{Binding CleanAllCommand}"
                            IsEnabled="{Binding IsCleaning, Converter={StaticResource InverseBoolConverter}}"
                            Background="#F87171" BorderBrush="#F87171" Foreground="#0F172A" Margin="6,0,0,0" Height="40">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Clean" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Clean All" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </StackPanel>

            <!-- Logs / Terminal Details -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="{Binding StatusText}" Foreground="White" FontWeight="Bold" FontSize="14" Margin="0,0,0,6"/>
                    <ProgressBar Value="{Binding ProgressValue}" Minimum="0" Maximum="100" Height="8" Foreground="#F87171" Background="#334155"/>
                </StackPanel>

                <TextBlock Grid.Row="1" Text="Cleaner Operation Logs" Foreground="#94A3B8" FontWeight="SemiBold" FontSize="12" Margin="0,4,0,8"/>

                <Border Grid.Row="2" Background="#0B0F19" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="12">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBox Text="{Binding CleanerLogs, Mode=OneWay}"
                                 Foreground="#34D399"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 FontFamily="Consolas"
                                 FontSize="12"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"/>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
